/// Modelo para Pontos Registrados
class Registry {
    constructor(id, userId, timeRegistered) {
        this.id = id;
        this.userId = userId;
        this.timeRegistered = timeRegistered;
    }
}

export default Registry;
