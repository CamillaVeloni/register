import React from 'react'; 
import { View, Text, StyleSheet } from 'react-native'; 

const ListItem = ({ name, time,  }) => { 
    return ( 
     <View> 
         <Text>text</Text>
     </View>
    );
};

const styles = StyleSheet.create({});

export default ListItem;
